import { SortnumberspipePipe } from './sortnumberspipe.pipe';

describe('SortnumberspipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortnumberspipePipe();
    expect(pipe).toBeTruthy();
  });
});
